services:
  openfy-server:
    build:
      context: ./server
    ports:
      - "8000:8000"
    volumes:
      - openfy_data:/data
    environment:
      - OPENFY_ALLOWED_ORIGINS=*

volumes:
  openfy_data:
